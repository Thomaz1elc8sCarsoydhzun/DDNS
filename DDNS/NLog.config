<?xml version="1.0" encoding="utf-8" ?>
<nlog>
  <targets>
    <target name="file" xsi:type="File" fileName="ddns.log" layout="${longdate}|${level}|${message}" />
  </targets>
  <rules>
    <logger name="*" minlevel="Info" writeTo="file" />
  </rules>
</nlog>
